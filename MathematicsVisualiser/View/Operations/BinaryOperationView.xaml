<UserControl x:Class="MathematicsVisualiser.View.Operations.BinaryOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:operators="clr-namespace:MathematicsVisualiser.View.Operators"
             xmlns:view="clr-namespace:MathematicsVisualiser.View"
             mc:Ignorable="d"
             d:DesignHeight="300"
						 d:DesignWidth="300">
	<UserControl.Resources>

		<view:DebugConverter x:Key="DebugConverter"/>
	</UserControl.Resources>

	<Grid>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" MaxWidth="50"/>
			<ColumnDefinition Width="24"/>
			<ColumnDefinition Width="*" MaxWidth="50"/>
			<ColumnDefinition Width="24"/>
			<ColumnDefinition Width="*" MaxWidth="50"/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<operators:UnaryOperatorView Grid.Column="0" DataContext="{Binding ., Converter={StaticResource ResourceKey=DebugConverter}}"/>

		<operators:BinaryOperatorView Grid.Column="1" DataContext="{Binding Path=SecondOperator}"/>

		<ContentControl Grid.Column="2" Content="{Binding SecondOperand}"/>

		<Button Grid.Column="3" Content="=" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Command="{Binding CalculateCommand}" Height="20" Width="20"/>

		<ContentControl Grid.Column="4" Content="{Binding ResultOperand}"/>

	</Grid>
</UserControl>
